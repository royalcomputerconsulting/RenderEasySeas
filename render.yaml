services:
  - type: web
    runtime: static        # or env: static on older examples, but runtime: static is current
    name: easyseas-production

    buildCommand: bun install && bun run build
    staticPublishPath: dist

    previews:
      generation: automatic